# Ví dụ adapter Torch

Việc ghép NanoDet và YOLO26 được kiểm tra bằng mock backend và tensor tổng hợp. Chạy ảnh thật dùng
CLI ảnh chung khi có runtime đã compile và artifact cục bộ phù hợp. Production không thay bằng
detection tổng hợp. Xem [model và artifact](../../docs/Vn/models.md).
