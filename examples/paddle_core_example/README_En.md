# Paddle adapter examples

The PicoDet adapter has synthetic contract tests, but a real Paddle image example remains gated
until the Paddle Inference backend and an exact model artifact are implemented and validated.
There is no fake runtime fallback. See [backends](../../docs/En/backends.md).
