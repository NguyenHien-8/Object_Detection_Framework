# Paddle Inference backend

Planned. `ODF_ENABLE_PADDLE=ON` currently fails configuration explicitly. The PicoDet adapter in
`frameworks/paddle` exists independently of this missing runtime. See the
[backend matrix](../../docs/En/backends.md).
